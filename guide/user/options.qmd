---
title: "KIMMDY Options"
execute:
  echo: false
---

## All Options

The following is a list of the options that can be set in the `kimmdy.yml` file.
It includes reactions currently available in KIMMDY as plugins.
Nested options are separated by a `.`.
`*` denotes an arbitrary name for a section.
The key for a section is **bold**.

```{python}
from kimmdy.schema import create_schemes, generate_markdown_table, get_all_plugin_schemas
from kimmdy.config import cast_config_dict_types, get_config_dict
from pathlib import Path

types, defaults = create_schemes()
p = Path('./example/example_ala/kimmdy.yml')
d = get_config_dict(p)
cast_config_dict_types(d)
```


```{python}
#| output: asis
table = generate_markdown_table(schema)
print(table)

for schema in get_all_plugin_schemas():
  table = generate_markdown_table(schema, "reactions", append=True)
  print(table)
```

```{python}
#| eval: false
#| echo: false

# for debugging
from kimmdy.config import Config, get_config_dict
from pathlib import Path

config = Config(Path('kimmdy.yml'))
```

## Example `kimmdy.yml` Files

```{.yaml filename='kimmdy.yml'}
dryrun: false
iterations: 100
name: 'hat_tf_000'
gromacs_alias: 'gmx'
top: 'Ala_out.top'
gro: 'npt.gro'
ndx: 'index.ndx'
mds:
  equilibrium:
    mdp: 'md.mdp'
  relax:
    mdp: 'md_slow.mdp'
changer:
  coordinates:
    md: 'relax'      
reactions:
  hat_reaction:
    frequency_factor: 100000000
    h_cutoff: 3
    polling_rate: 1

sequence:
- equilibrium
- mult: 2
  tasks:
  - equilibrium
  - reactions
```



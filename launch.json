{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "KIMMDY",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/kimmdy/cmd.py",
            // "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/example/short_example/"
        }
    ]
}
